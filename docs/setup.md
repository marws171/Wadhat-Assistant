# Setup Instructions for Wadhat

## Requirements
- Python 3.8+
- GPU (optional for better performance)
- Google Colab with T4 GPU (for running the quantized model)

## Installation
1. Clone the repository:
   ```bash
   git clone https://github.com/your_username/Wadhat-AI.git
   cd Wadhat-AI
